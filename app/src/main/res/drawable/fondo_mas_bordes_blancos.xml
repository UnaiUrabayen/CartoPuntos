<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/cp_light_primary"/> <!-- Fondo transparente -->
    <stroke
        android:width="2dp"
        android:color="@android:color/white"/> <!-- Borde blanco -->
    <corners android:radius="3dp"/> <!-- Esquinas redondeadas opcionales -->
</shape>
