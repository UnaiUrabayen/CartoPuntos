<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fondo gris sin opacidad -->
    <solid android:color="#CCCCCC" />

    <!-- Borde negro -->
    <stroke
        android:width="2dp"
        android:color="#000000" />

    <!-- Esquinas redondeadas -->
    <corners android:radius="8dp" />
</shape>
